@* _StudentLayout.cshtml *@
@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] | Student Panel</title>

    <!-- CSS -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
    <link rel="stylesheet" href="~/css/Profile.css" />
    <link rel="stylesheet" href="~/css/darkmode.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" />

    @RenderSection("Styles", required: false)
</head>
<body>
    <!-- Overlay -->
    <div class="sidebar-overlay"></div>

    <!-- LEFT SIDEBAR -->
    <aside class="admin-sidebar">
        <div class="scroll-wrapper" style="flex:1; overflow-y:auto; scrollbar-width:none; -ms-overflow-style:none;">
            <style>
                .scroll-wrapper::-webkit-scrollbar {
                    display: none;
                }
            </style>

            <!-- Logo -->
            <div class="admin-logo-header">
                <img src="~/images/ctu-logo.png" class="admin-logo" />
                <div class="admin-logo-text">BALAMBAN CAMPUS</div>
            </div>

            <!-- Menu -->
            <nav class="admin-menu">
                <a asp-action="Dashboard" asp-controller="StudentPanel" class="menu-link" id="dashboardBtn">
                    <i class="bi bi-speedometer2"></i> <span>Dashboard</span>
                </a>
                <a asp-action="Tasks" asp-controller="StudentPanel" class="menu-link">
                    <i class="bi bi-clipboard-check-fill"></i> <span>Tasks</span>
                </a>
                <a asp-action="Report" asp-controller="StudentPanel" class="menu-link">
                    <i class="bi bi-file-earmark-text-fill"></i> <span>Report</span>
                </a>
                <hr />
            </nav>

            <!-- Logout -->
            <div class="sidebar-logout">
                <form id="logoutForm" asp-controller="Account" asp-action="Logout" method="post" class="d-none">
                    @Html.AntiForgeryToken()
                </form>
                <a href="javascript:void(0);" onclick="document.getElementById('logoutForm').submit();" class="btn-logout">
                    <i class="bi bi-box-arrow-right"></i> <span>Logout</span>
                </a>
            </div>
        </div>
    </aside>

    <!-- TOP BAR -->
    <header class="admin-topbar d-flex justify-content-between align-items-center">
        <div class="d-flex align-items-center gap-2">
            <button class="btn btn-sm btn-light d-none d-md-inline-flex me-2" id="desktopSidebarToggle">
                <i class="bi bi-list"></i>
            </button>
            <button class="btn btn-sm btn-light d-md-none me-2" id="sidebarToggle">
                <i class="bi bi-list"></i>
            </button>
            <i class="bi bi-person-circle me-2"></i>
            <span class="topbar-title">@ViewData["Title"]</span>
        </div>

        <div class="d-flex align-items-center gap-2">
            <!-- Dark Mode Toggle -->
            <div id="themeToggle" style="cursor:pointer;">
                <i class="bi bi-moon-fill fs-5" id="themeIcon"></i>
            </div>

            <!-- Notifications -->
            <div class="position-relative" id="notifBell" style="cursor:pointer;">
                <i class="bi bi-bell-fill fs-5"></i>
                <span class="notif-dot"></span>
            </div>

            <!-- Small Topbar Profile Picture -->
            <img src="/images/ctu-logo.png" id="topbarProfilePic" title="Profile Picture" />

            <!-- Profile Dropdown -->
            <div class="dropdown">
                <div class="d-flex align-items-center gap-2 dropdown-toggle" role="button" id="profileDropdown" data-bs-toggle="dropdown" aria-expanded="false" style="cursor:pointer;">
                    <div class="d-flex flex-column" style="font-size:0.75rem;">
                        <strong>Student User</strong>
                        <span>Student</span>
                    </div>
                </div>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="profileDropdown">
                    <li><a class="dropdown-item" href="javascript:void(0);" id="viewProfileBtn">View Profile</a></li>
                    <li><a class="dropdown-item" href="javascript:void(0);" onclick="document.getElementById('logoutForm').submit();">Logout</a></li>
                </ul>
            </div>
        </div>
    </header>

    <!-- MAIN CONTENT -->
    <main class="admin-content" id="adminMainContent">
        @RenderBody()
    </main>

    <!-- PROFILE TEMPLATE (Complete) -->
    <script type="text/template" id="profileTemplate">
        <div class="container py-4">
            <!-- Profile Picture with upload -->
            <div class="profile-pic-wrapper mb-4 position-relative" style="width:140px; height:140px;">
                <img src="/images/ctu-logo.png" id="profileImage" class="rounded-circle border border-3 border-white" style="width:140px;height:140px;object-fit:cover;">
                <div class="profile-pic-overlay d-flex justify-content-center align-items-center"
                     style="position:absolute; top:0; left:0; width:100%; height:100%; border-radius:50%; background:rgba(0,0,0,0.3); opacity:0; cursor:pointer; transition:opacity 0.2s;">
                    <i class="bi bi-camera-fill text-white fs-4"></i>
                </div>
                <div class="change-pic-icon position-absolute d-flex justify-content-center align-items-center"
                     style="width:36px; height:36px; bottom:0; right:0; background:rgba(0,0,0,0.2); border-radius:50%; border:2px solid white; cursor:pointer;">
                    <i class="bi bi-camera-fill text-white"></i>
                </div>
                <input type="file" id="profilePicInput" accept="image/*" style="display:none;">
            </div>

            <div class="card shadow-sm p-4" style="max-width:1200px;">
                <div class="flex-grow-1 w-100">
                    <!-- Row 1 -->
                    <div class="row mb-3">
                        <div class="col-md-4">
                            <label for="fullName" class="form-label fw-bold">Full Name</label>
                            <input type="text" id="fullName" class="form-control" value="John Doe">
                        </div>
                        <div class="col-md-4">
                            <label for="facebook" class="form-label fw-bold">Facebook</label>
                            <input type="text" id="facebook" class="form-control" value="facebook.com/johndoe">
                        </div>
                        <div class="col-md-4">
                            <label for="company" class="form-label fw-bold">Company</label>
                            <input type="text" id="company" class="form-control" value="CTU Balamban">
                        </div>
                    </div>

                    <!-- Row 2 -->
                    <div class="row mb-3">
                        <div class="col-md-4">
                            <label for="phone" class="form-label fw-bold">Phone Number</label>
                            <input type="text" id="phone" class="form-control" value="09171234567">
                        </div>
                        <div class="col-md-4">
                            <label for="address" class="form-label fw-bold">Address</label>
                            <input type="text" id="address" class="form-control" value="123 Main St, Balamban">
                        </div>
                        <div class="col-md-4">
                            <label for="password" class="form-label fw-bold">Password</label>
                            <div class="input-group">
                                <input type="password" id="password" class="form-control" value="password123" />
                                <span class="input-group-text" id="togglePassword" style="background:transparent; border-left:0; cursor:pointer;">
                                    <i class="bi bi-eye-slash"></i>
                                </span>
                            </div>
                        </div>
                    </div>

                    <!-- Row 3 -->
                    <div class="row mb-3">
                        <div class="col-md-4">
                            <label for="contactPerson" class="form-label fw-bold">Contact Person</label>
                            <input type="text" id="contactPerson" class="form-control" value="John Smith">
                        </div>
                        <div class="col-md-4">
                            <label for="studentId" class="form-label fw-bold">Student ID</label>
                            <input type="text" id="studentId" class="form-control" value="202300123">
                        </div>
                        <div class="col-md-4">
                            <label for="program" class="form-label fw-bold">Program</label>
                            <input type="text" id="program" class="form-control" value="BSIT">
                        </div>
                    </div>

                    <!-- Row 4 -->
                    <div class="row mb-3">
                        <div class="col-md-4">
                            <label for="birthdate" class="form-label fw-bold">Birthdate</label>
                            <input type="date" id="birthdate" class="form-control" value="2005-01-15">
                        </div>
                        <div class="col-md-4">
                            <label for="lastEmail" class="form-label fw-bold">Last Email</label>
                            <input type="email" id="lastEmail" class="form-control" value="student@ctu.edu.ph">
                        </div>
                        <div class="col-md-4">
                            <!-- Empty for spacing -->
                        </div>
                    </div>

                    <div class="text-end">
                        <button class="btn" id="saveProfileBtn" style="background-color:#07B618; border-color:#07B618; color:white;">
                            Save Changes
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </script>

    <!-- NOTIFICATION TEMPLATE -->
    <script type="text/template" id="notifTemplate">
        <div class="container py-4">
            <h4 class="mb-4">Notification History</h4>

            <!-- Example Notification Card 1 -->
            <div class="card shadow-sm mb-4 p-4 d-flex justify-content-between align-items-center"
                 style="max-width:90%; font-size:1.1rem; border-radius:25px; transform: scale(1.02);">
                <div class="d-flex justify-content-between w-100 align-items-center">
                    <div>
                        <h5 class="fw-bold mb-1">Task Reminder</h5>
                        <small>Your daily report is due tomorrow.</small>
                    </div>
                    <button class="btn btn-outline-primary btn-sm viewNotifBtn" data-bs-toggle="modal" data-bs-target="#reportModal"
                            data-title="Task Reminder" data-message="Please submit your daily report before 5:00 PM tomorrow.">
                        <i class="bi bi-eye fs-5"></i>
                    </button>
                </div>
            </div>

            <!-- Example Notification Card 2 -->
            <div class="card shadow-sm mb-4 p-4 d-flex justify-content-between align-items-center"
                 style="max-width:90%; font-size:1.1rem; border-radius:25px; transform: scale(1.02);">
                <div class="d-flex justify-content-between w-100 align-items-center">
                    <div>
                        <h5 class="fw-bold mb-1">Report Status</h5>
                        <small>Your MOA has been approved.</small>
                    </div>
                    <button class="btn btn-outline-primary btn-sm viewNotifBtn" data-bs-toggle="modal" data-bs-target="#reportModal"
                            data-title="Report Status" data-message="Your Memorandum of Agreement has been approved by the coordinator.">
                        <i class="bi bi-eye fs-5"></i>
                    </button>
                </div>
            </div>
        </div>
    </script>

    <!-- REPORT DETAILS MODAL -->
    <div class="modal fade" id="reportModal" tabindex="-1" aria-labelledby="reportModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-centered">
            <div class="modal-content rounded-4">
                <div class="modal-header">
                    <h5 class="modal-title" id="reportModalLabel">Notification Details</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <h3 class="fw-bold mb-3" id="modalTitle">Title</h3>
                    <hr>
                    <div style="border:1px solid #ccc; border-radius:10px; padding:15px; background:#f8f9fa;">
                        <p id="modalMessage" style="font-size:1.05rem; line-height:1.5;">Message goes here.</p>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>

    <!-- JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const sidebar = document.querySelector('.admin-sidebar');
        const desktopToggle = document.getElementById('desktopSidebarToggle');
        const mobileToggle = document.getElementById('sidebarToggle');
        const overlay = document.querySelector('.sidebar-overlay');

        if(window.innerWidth >= 768) sidebar.classList.add('expanded');

        desktopToggle?.addEventListener('click', ()=>sidebar.classList.toggle('expanded'));
        mobileToggle?.addEventListener('click', ()=>{
            sidebar.classList.toggle('expanded');
            overlay.classList.toggle('active');
        });
        overlay?.addEventListener('click', ()=>{
            sidebar.classList.remove('expanded');
            overlay.classList.remove('active');
        });

        // ========== SPA Content Switching ==========
        const mainContent = document.getElementById("adminMainContent");
        const viewProfileBtn = document.getElementById("viewProfileBtn");
        const topbarProfilePic = document.getElementById("topbarProfilePic");
        const originalDashboardHTML = mainContent.innerHTML;

        // Profile SPA
        function openProfileSPA(){
            mainContent.style.opacity = 0;
            setTimeout(()=>{
                const template = document.getElementById("profileTemplate").innerHTML;
                mainContent.innerHTML = template;
                document.querySelector(".topbar-title").textContent = "Profile";

                const profileImage = document.getElementById("profileImage");
                const profileOverlay = document.querySelector(".profile-pic-overlay");
                const profilePicInput = document.getElementById("profilePicInput");

                profileOverlay.addEventListener("click", ()=> profilePicInput.click());
                const changePicIcon = document.querySelector(".change-pic-icon");
                changePicIcon.addEventListener("click", ()=> profilePicInput.click());

                profilePicInput.addEventListener("change", (e)=>{
                    if(e.target.files && e.target.files[0]){
                        const reader = new FileReader();
                        reader.onload = function(evt){
                            profileImage.src = evt.target.result;
                            topbarProfilePic.src = evt.target.result;
                        }
                        reader.readAsDataURL(e.target.files[0]);
                    }
                });

                // Password toggle
                const passwordInput = document.getElementById('password');
                const togglePassword = document.getElementById('togglePassword');
                if (togglePassword) {
                    togglePassword.addEventListener('click', () => {
                        const type = passwordInput.type === 'password' ? 'text' : 'password';
                        passwordInput.type = type;
                        const icon = togglePassword.querySelector('i');
                        icon.classList.toggle('bi-eye');
                        icon.classList.toggle('bi-eye-slash');
                    });
                }

                mainContent.style.opacity = 1;
            },200);
        }

        viewProfileBtn?.addEventListener("click", openProfileSPA);

        // Dashboard button
        document.getElementById("dashboardBtn")?.addEventListener("click", ()=>{
            mainContent.style.opacity = 0;
            setTimeout(()=>{
                mainContent.innerHTML = originalDashboardHTML;
                document.querySelector(".topbar-title").textContent = "Dashboard";
                mainContent.style.opacity = 1;
            },200);
        });

        // ========== Notifications SPA ==========
        const notifBell = document.getElementById("notifBell");
        notifBell?.addEventListener("click", function() {
            mainContent.style.opacity = 0;
            setTimeout(() => {
                mainContent.innerHTML = document.getElementById("notifTemplate").innerHTML;
                document.querySelector(".topbar-title").textContent = "Notifications";

                // Attach click handlers to the "view" buttons inside the template
                document.querySelectorAll(".viewNotifBtn").forEach(btn => {
                    btn.addEventListener("click", function () {
                        document.getElementById("modalTitle").textContent = this.dataset.title;
                        document.getElementById("modalMessage").textContent = this.dataset.message;
                    });
                });

                mainContent.style.opacity = 1;
            }, 200);
        });

        // ========== Dark Mode ==========
        const themeToggle = document.getElementById("themeToggle");
        const themeIcon = document.getElementById("themeIcon");
        if(localStorage.getItem("darkMode")==="true"){
            document.body.classList.add("dark-mode");
            themeIcon.classList.replace("bi-moon-fill","bi-sun-fill");
        }
        themeToggle?.addEventListener("click", ()=>{
            document.body.classList.toggle("dark-mode");
            const isDark = document.body.classList.contains("dark-mode");
            themeIcon.classList.toggle("bi-moon-fill",!isDark);
            themeIcon.classList.toggle("bi-sun-fill",isDark);
            localStorage.setItem("darkMode",isDark);
        });
    </script>

    @RenderSection("Scripts", required: false)
</body>
</html>